<h2>Foto albums</h2>

<ul class="albums">
<% for album in @albums %>
	<li>
		<a href="/gallery/<%= album.id %>">
			<img src="/image/<%= album_photo album.id %>/large" alt="<%= album.name %>">
			<p><%= album.name %></p>
		</a>
	</li>
<% end %>
</ul>
<script>
	jQuery(function($) {
		$.supersized({
			slides	:  [ { image : '/image/<%= @photo.id %>/huge' } ]					
		});
	});
</script>

<style type="text/css">
	*{ margin: 0; padding: 0; }
	
	header { display: none; }

	#wrapper { background:rgb(0,0,0); background:rgba(0,0,0,0.75); }
	#supersized-loader { position: absolute; top: 49.5%; left: 49.5%; z-index: 10; width: 24px; height: 24px; text-indent: -999em; background-image: url(../img/progress.gif); }
	#supersized { position:fixed; left:0; top:0; overflow:hidden; z-index:-999; height:100%; width:100%; }
	#supersized img{ position:relative; outline:none; border:none; -ms-interpolation-mode:bicubic; image-rendering: optimizeQuality; }
</style>

<a href="/gallery/<%= @album.id %>">&laquo; terug naar '<%= @album.path %>'</a><br><br>

<h2><%= @album.name %> - <%= @photo.name %></h2>

<a href="/image/<%= @photo.id %>/original" target="_blank">download ware grootte</a>

<a href="/gallery/<%= @album.id %>/image/<%= @photo_prev.id %>" title="<%= @photo_prev.name %>">vorige</a>
 - 
<a href="/gallery/<%= @album.id %>/image/<%= @photo_next.id %>" title="<%= @photo_next.name %>">volgende</a>
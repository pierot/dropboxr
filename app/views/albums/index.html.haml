%h1 Fotos Noort

%ul.media-grid
  - @albums.each do |album|
    %li
      = link_to (image_tag image_path(album_photo(album.id), 'large'), :alt => album.name, :class => 'thumbnail'), album_path(album.slug)
      = link_to album.name, album_path(album.slug)
      %span.label.success New
